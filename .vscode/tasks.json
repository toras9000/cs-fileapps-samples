{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${file}",
                "--output",
                "${env:TEMP}/cs-filebased/${relativeFileDirname}/${fileBasenameNoExtension}"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}